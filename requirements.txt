fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.20
sqlalchemy==2.0.36
itsdangerous==2.2.0

# основной способ сбора — обычные HTTP-запросы к 2ГИС
httpx>=0.28.1

# запасной способ: если 2ГИС перестанет отдавать страницы простому клиенту,
# сбор сам переключится на настоящий браузер.
# Версию не пинуем: она жёстко связана со сборкой браузера,
# которую ставит `playwright install chromium`
playwright>=1.49

# опционально: генерация сообщений через Claude (без ключа работает фолбэк)
anthropic>=0.42.0
